# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# 環境変数（APIキーなどの秘密情報）
.env
.env.*

# 問題画像（サイズが大きいためGit管理外・別途バックアップ）
public/samplequestions/
samplequestions/
screenshot_zihai_before.png

# Claude Code 開発メモ
CLAUDE.md

# Claude Code 個人用設定（Git管理外）
.claude/settings.local.json

# 画像スキャンのローカル進捗状態
scripts/progress.json

# X投稿下書きのプレビュー出力（実行のたびに生成される一時ファイル）
scripts/tweet-drafts-out/

# ゴミ箱（削除ファイルの一時退避先）
.trash/

# ローカル専用ツール（雀魂の牌譜取得スクリプト等）と取り込んだ牌譜（tools-local/paifu/）。
# 前者は規約上グレーな外部ツールの改変物、後者は対局者名を含むため公開リポジトリに入れない
# （docs/user-problems-plan.md 9章）
tools-local/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
